#include <stdio.h>

int main(){
    printf("veuillez entrer un nombre entier : ");
    int nbr;
    scanf("%d",&nbr);
    if (nbr % 2 == 0)
    {
        printf("%d est paire \n",nbr);
    }
    else
    {
        printf("%d est paire \n",nbr);
    }
}